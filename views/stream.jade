extends layout

block content
  div#top-bar
    div#logo
  div#hint
    h2 #{location_name}
  div#info
    h3 Prevalent mood Here: #{emotion}
    h4 Top 10 hashtags: #{top_tags}
  div#content
    ul#image-list
      each media in media_list
        li
          div
            - hashtags = media['tags'].join(', ')
            img.insta-image(src="#{media['images']['standard_resolution']['url']}", title="#{hashtags}")